"""
improve — self-improvement engine modules.

The orchestrator script lives at /improve_with_claude_code.py (path is fixed
because graph.py invokes it as a subprocess). This package holds extracted
helpers that the orchestrator and the standalone scripts/verify_quality.py
both consume.
"""
